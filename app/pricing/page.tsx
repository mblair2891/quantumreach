import { PublicLayout } from "@/components/public-layout/layout";
import { SaaSPlanKeys, planEntitlements } from "@/lib/saas/entitlements";
import { Button } from "@/components/ui/button";
export default function PricingPage(){return <PublicLayout><section className="mx-auto max-w-7xl px-6 py-16"><h1 className="text-4xl font-semibold">SaaS plans</h1><p className="mt-3 max-w-2xl text-slate-700">Starter, Professional, Agency, and Enterprise entitlement foundations. Final commercial pricing is intentionally configured outside source control.</p><div className="mt-8 grid gap-4 md:grid-cols-4">{SaaSPlanKeys.map((plan)=><article key={plan} className="rounded-3xl border bg-white p-5 shadow-sm"><h2 className="font-semibold text-slate-950">{plan}</h2><p className="mt-2 text-sm text-slate-700">Contacts: {planEntitlements[plan].contacts}</p><p className="text-sm text-slate-700">Users: {planEntitlements[plan].users}</p><p className="text-sm text-slate-700">Managed domains: {planEntitlements[plan].managedDomains}</p><Button className="mt-4" disabled>Upgrade flow pending Stripe setup</Button></article>)}</div></section></PublicLayout>}
