import Link from "next/link"; import { requireUserProfile } from "@/lib/auth/rbac"; import { prisma } from "@/lib/db/prisma"; import { FunnelProgress } from "@/components/funnel/progress"; import { redirect } from "next/navigation"; import { trackFunnelEvent } from "@/lib/customer-journey/funnel";
const priorityLabel: Record<string,string>={STANDARD:"Standard setup",PRIORITY:"Priority setup",EXPEDITED:"Expedited setup"};
export default async function Confirmation(){const user=await requireUserProfile();const setup=await prisma.infrastructureOrder.findFirst({where:{order:{userId:user.id}},include:{order:true},orderBy:{createdAt:"desc"}});if(!setup)redirect("/setup/infrastructure");await trackFunnelEvent("FUNNEL_COMPLETED",{userId:user.id,customerOrderId:setup.customerOrderId});const payment=setup.order.paymentStatus==="PAID"?"Payment confirmed":"Your order is awaiting confirmation";return <main className="mx-auto max-w-3xl px-5 py-12 sm:py-20"><FunnelProgress current={4}/><div className="rounded-2xl border bg-white p-7 sm:p-10"><p className="font-bold tracking-widest text-indigo-600">YOU’RE ON YOUR WAY</p><h1 className="mt-3 text-4xl font-semibold">Your Quantum Reach setup is now in progress.</h1><p className="mt-4 text-slate-600">You’ve completed the program, package, and setup-speed choices. We’ll guide you through anything we still need.</p><dl className="mt-8 space-y-4 border-y py-6"><div className="flex justify-between gap-4"><dt>Program</dt><dd className="font-medium">Quantum Reach Agency Program</dd></div><div className="flex justify-between gap-4"><dt>Infrastructure</dt><dd className="font-medium">{setup.selectedProductKey.replaceAll("_"," ")}</dd></div><div className="flex justify-between gap-4"><dt>Setup priority</dt><dd className="font-medium">{priorityLabel[setup.priority]}</dd></div><div className="flex justify-between gap-4"><dt>Payment</dt><dd className="font-medium">{payment}</dd></div><div className="flex justify-between gap-4"><dt>Current status</dt><dd className="font-medium">{setup.currentStage}</dd></div></dl><h2 className="mt-8 text-xl font-semibold">What happens next</h2><p className="mt-2 text-slate-600">Quantum Reach reviews your setup and will surface any information needed from you. Keep an eye on your setup status for the latest progress.</p><Link className="mt-7 flex min-h-12 items-center justify-center rounded-xl bg-indigo-600 font-semibold text-white" href="/setup/status">View Setup Status</Link></div></main>}
