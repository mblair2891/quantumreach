import Link from "next/link";import { requireSubscriberWorkspaceAccess } from "@/lib/saas/access";import { ENTITLEMENT_KEYS, remaining, pricingLabel } from "@/lib/sending-infrastructure/catalog";import { getSubscriberSendingSnapshot } from "@/lib/sending-infrastructure/operational";
export default async function SendingInfrastructurePage(){const {workspace}=await requireSubscriberWorkspaceAccess();const s=await getSubscriberSendingSnapshot(workspace.id);const e=s.effective;const stats=[['Domains',s.domains.length,e[ENTITLEMENT_KEYS.MANAGED_DOMAIN_ALLOWANCE]],['Mailboxes',s.mailboxes.length,e[ENTITLEMENT_KEYS.MAILBOX_ALLOWANCE]],['Sender identities',s.senders.length,e[ENTITLEMENT_KEYS.SENDER_IDENTITY_ALLOWANCE]],['Monthly sends',s.usage.sentMessageCount,e[ENTITLEMENT_KEYS.MONTHLY_SEND_ALLOWANCE]]];return <main className="space-y-6"><h1>My Sending Infrastructure</h1>{!s.sendingPackage?<p>No sending infrastructure package is active.</p>:<section><h2>{s.sendingPackage.commerceProduct.name}</h2><p>{pricingLabel(s.sendingPackage.commerceProduct)}</p></section>}<section className="grid gap-3 md:grid-cols-4">{stats.map(([label,used,allowed])=><article key={label as string} className="rounded border p-4"><h2>{label}</h2><p>{Number(used)} / {Number(allowed||0)}</p><p>Remaining: {remaining(Number(allowed||0),Number(used))}</p></article>)}</section><nav><Link href="/dashboard/sending/domains">Domains</Link> · <Link href="/dashboard/sending/mailboxes">Mailboxes</Link> · <Link href="/dashboard/sending/senders">Senders</Link> · <Link href="/dashboard/sending/usage">Usage</Link> · <Link href="/dashboard/sending/deliverability">Deliverability</Link></nav></main>}
