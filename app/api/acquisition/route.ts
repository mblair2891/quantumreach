import { NextRequest, NextResponse } from "next/server"; import { randomUUID } from "crypto"; import { recordAcquisition } from "@/lib/customer-journey/service";
export async function POST(request:NextRequest){const body=await request.json();const id=request.cookies.get("qr_acquisition")?.value??randomUUID();await recordAcquisition({anonymousId:id,landingPage:"/start",source:body.utm_source,medium:body.utm_medium,campaign:body.utm_campaign,content:body.utm_content,term:body.utm_term});const response=NextResponse.json({ok:true});response.cookies.set("qr_acquisition",id,{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge:60*60*24*60});return response}
