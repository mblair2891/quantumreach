import Link from "next/link";
import { PublicLayout } from "@/components/public-layout/layout";
const sections = [{title:"Opportunity",body:"Build a responsible AI marketing and automation practice with a repeatable operating system—not unverified income promises."},{title:"The system",body:"Program training, Quantum Reach software, managed sending infrastructure, and a learning community work together while each entitlement remains distinct."},{title:"How it works",body:"Join the program, complete your foundations, choose the sending package that fits your launch, then progress through a transparent setup queue."},{title:"Who it is for",body:"Operators who want to learn, implement compliant outreach, and build client delivery systems with clear ownership."}];
export default function StartPage({searchParams}:{searchParams:Record<string,string|undefined>}) { const query=new URLSearchParams(Object.entries(searchParams).filter(([,v])=>v) as [string,string][]).toString(); return <PublicLayout><main className="mx-auto max-w-6xl px-6 py-16"><p className="text-sm font-semibold text-indigo-600">QUANTUM REACH PROGRAM</p><h1 className="mt-3 max-w-4xl text-5xl font-semibold tracking-tight">Learn the agency system. Launch with software and sending infrastructure built for the work.</h1><p className="mt-6 max-w-3xl text-lg text-slate-600">A practical path from education to a ready workspace, with setup stages and responsibilities visible at every step.</p><div className="mt-8 flex gap-3"><Link className="rounded-lg bg-slate-950 px-5 py-3 font-medium text-white" href={`/join${query?`?${query}`:""}`}>Join the program</Link><a className="rounded-lg border px-5 py-3 font-medium" href="#how-it-works">See how setup works</a></div><section className="mt-12 rounded-2xl bg-slate-950 p-10 text-white"><p className="text-sm text-slate-300">VIDEO OVERVIEW</p><div className="mt-4 flex min-h-48 items-center justify-center rounded-xl border border-slate-700 bg-slate-900 text-slate-300">Video URL configuration will appear here when configured.</div></section><div className="mt-16 grid gap-6 md:grid-cols-2">{sections.map((s)=><section id={s.title==="How it works"?"how-it-works":undefined} key={s.title} className="rounded-2xl border bg-white p-7"><h2 className="text-xl font-semibold">{s.title}</h2><p className="mt-3 text-slate-600">{s.body}</p></section>)}</div><section className="mt-12"><h2 className="text-2xl font-semibold">Questions, answered</h2><p className="mt-3 text-slate-600">Infrastructure setup does not bypass compliance, domain verification, or provider readiness. Pricing is shown only when configured.</p></section></main></PublicLayout> }
