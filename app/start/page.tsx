import Link from "next/link";
import { AcquisitionCapture } from "./acquisition";
import { FunnelShell } from "@/components/funnel/shell";
import { VslPlayer } from "@/components/funnel/vsl-player";
const feature = [["The operating system", "How training, software, and managed outreach fit into one responsible system."], ["A practical launch path", "The decisions that turn an agency ambition into a repeatable operating rhythm."], ["Support that scales", "Where expert infrastructure support belongs as your outreach program grows."]];
export default function StartPage({ searchParams }: { searchParams: Record<string, string | undefined> }) { const query = new URLSearchParams(Object.entries(searchParams).filter(([, value]) => value) as [string, string][]).toString(); const join = `/join${query ? `?${query}` : ""}`; return <FunnelShell><AcquisitionCapture params={searchParams}/><main className="mx-auto max-w-6xl px-5 pb-20 pt-10 sm:px-8 sm:pt-16"><section className="text-center"><p className="funnel-eyebrow">A system for building and running an AI-powered agency</p><h1 className="funnel-title mx-auto mt-5 max-w-5xl text-4xl font-semibold leading-[1.04] sm:text-6xl">Build the agency your ambition has been waiting for.</h1><p className="funnel-copy mx-auto mt-6 max-w-2xl text-base sm:text-lg">See the practical path from learning the model to operating a focused agency with the systems and managed infrastructure to support it.</p></section><section className="relative mx-auto mt-10 max-w-5xl sm:mt-12"><div className="absolute -inset-5 -z-10 rounded-[2rem] bg-indigo-300/30 blur-3xl"/><div className="rounded-[1.4rem] border border-slate-300 bg-slate-900 p-2 shadow-[0_28px_65px_-25px_rgba(15,23,42,.65)]"><VslPlayer /></div></section><div className="mt-8 text-center"><Link className="funnel-primary w-full sm:w-auto" href={join}>Join Quantum Reach <span className="ml-2">→</span></Link><p className="mt-4 text-sm text-slate-500">Watch the presentation, then take the next clear step. No technical setup required yet.</p></div><section className="mt-24"><div className="max-w-2xl"><p className="funnel-eyebrow">What you’ll discover</p><h2 className="funnel-title mt-3 text-3xl font-semibold sm:text-4xl">A clearer way to build momentum.</h2></div><div className="mt-8 grid gap-px overflow-hidden rounded-2xl border border-slate-200 bg-slate-200 md:grid-cols-3">{feature.map(([title, copy], index) => <article key={title} className="bg-white p-7"><span className="text-sm font-bold text-indigo-600">0{index + 1}</span><h3 className="mt-8 text-xl font-semibold text-slate-900">{title}</h3><p className="funnel-copy mt-3 text-sm">{copy}</p></article>)}</div></section><section className="mt-20 grid items-center gap-10 border-y border-slate-200 py-14 md:grid-cols-[.8fr_1.2fr]"><div><p className="funnel-eyebrow">Who this is for</p><h2 className="funnel-title mt-3 text-3xl font-semibold">Built for operators ready to own the work.</h2></div><ul className="space-y-4 text-slate-700">{["You want a serious, repeatable agency practice—not another disconnected tool.", "You value responsible outreach and clear operational ownership.", "You are ready to follow a guided path while building long-term capability."].map(item => <li className="flex gap-3" key={item}><span className="mt-1 text-indigo-600">✓</span><span>{item}</span></li>)}</ul></section><section className="mt-20"><p className="funnel-eyebrow">How it works</p><div className="mt-6 grid gap-6 md:grid-cols-3">{[["01", "Join the program", "Access the training, community, and software path."], ["02", "Choose your foundation", "Select managed infrastructure that matches your outreach goals."], ["03", "Follow your setup", "Stay informed as Quantum Reach guides your launch."]].map(([number,title,copy]) => <div key={number} className="border-l-2 border-indigo-200 pl-5"><span className="text-sm font-bold text-indigo-600">{number}</span><h3 className="mt-2 text-xl font-semibold">{title}</h3><p className="funnel-copy mt-2 text-sm">{copy}</p></div>)}</div></section><section className="funnel-card mt-20 p-7 sm:p-10"><p className="funnel-eyebrow">Questions, answered</p><h2 className="mt-3 text-2xl font-semibold">A thoughtful system, with a straightforward next step.</h2><p className="funnel-copy mt-3 max-w-3xl">The program focuses on learning and operating a responsible outreach system. Setup speed changes our handling priority, not compliance or external requirements.</p></section><section className="mt-16 rounded-3xl bg-slate-950 px-7 py-12 text-center text-white sm:px-12"><p className="text-xs font-bold uppercase tracking-[.2em] text-indigo-300">Your next move</p><h2 className="mt-4 text-3xl font-semibold tracking-tight sm:text-4xl">Ready to see what Quantum Reach can unlock?</h2><Link className="funnel-primary mt-7 bg-white text-slate-950 shadow-none hover:bg-indigo-50" href={join}>Join Quantum Reach <span className="ml-2">→</span></Link></section></main></FunnelShell>; }
