"use server";import { revalidatePath } from "next/cache";import { requireClientPortalAccess } from "@/lib/saas/access";import { recordAIUsage,selectWorkspaceAI } from "@/lib/ai/workspace-service";import type { AIProviderKey } from "@/lib/ai/workspace-providers";const v=(f:FormData,k:string)=>String(f.get(k)??"").trim();export async function selectClientAIAction(f:FormData){const{workspace,user}=await requireClientPortalAccess();const selection=await selectWorkspaceAI({workspaceId:workspace.id,provider:v(f,"provider") as AIProviderKey,model:v(f,"model"),clientAuthorized:true,clientId:user.id,feature:v(f,"feature")||"CLIENT_WORKFLOW",projectId:v(f,"projectId")||undefined});await recordAIUsage({workspaceId:workspace.id,credentialId:selection.credentialId,provider:selection.provider,model:selection.model,feature:selection.feature,status:"SELECTED",clientId:user.id,projectId:selection.projectId});revalidatePath("/portal/ai");}
