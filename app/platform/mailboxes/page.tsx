import { requireOperatorAccess } from "@/lib/admin/operator";import { prisma } from "@/lib/db/prisma";import { getMailboxProvider } from "@/lib/sending-infrastructure/providers";
export default async function PlatformMailboxesPage(){await requireOperatorAccess();const health=getMailboxProvider().getProviderHealth();const boxes=await prisma.managedMailbox.findMany({orderBy:{updatedAt:'desc'},take:100});return <main><h1>Mailbox Operations</h1><p>{health.state}: {health.safeMessage}</p>{boxes.length===0?<p>No persisted mailboxes exist.</p>:boxes.map(m=><article key={m.id}><h2>{m.emailAddress}</h2><p>Workspace: {m.workspaceId}; status: {m.status}; provider: {m.provider}; readiness: {m.provisioningStatus}; last health: {m.lastHealthCheckAt?.toISOString()??'Never'}</p></article>)}</main>}
