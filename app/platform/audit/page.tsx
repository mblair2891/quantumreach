import { PlatformPageTemplate } from "@/components/platform/page-template";
export default function Page(){return <PlatformPageTemplate title="Platform audit logs" description="Operator-only safe summaries for platform audit logs." items={['Safe summary','Status','Actions']}/>}
