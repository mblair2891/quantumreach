import { requireOperatorAccess } from "@/lib/admin/operator";import { entitlementMap, listCatalogProducts } from "@/lib/sending-infrastructure/operational";import { bootstrapCatalogAction, saveEntitlementAction, saveProductAction, setProductActiveAction } from "../catalog/actions";import { pricingLabel, ENTITLEMENT_KEYS } from "@/lib/sending-infrastructure/catalog";
export default async function PlatformSendingPackagesPage(){await requireOperatorAccess();const products=(await listCatalogProducts()).filter(p=>p.category==='SENDING_PACKAGE');return <main className="space-y-6"><h1>Sending Packages</h1><form action={bootstrapCatalogAction}><button>Seed Launch/Growth/Scale packages</button></form><section><h2>Create package</h2><form action={saveProductAction}><input name="key" placeholder="PACKAGE_KEY" required/><input name="name" placeholder="Package name" required/><input type="hidden" name="category" value="SENDING_PACKAGE"/><input type="hidden" name="billingInterval" value="MONTH"/><input type="hidden" name="active" value="true"/><input name="stripePriceId" placeholder="Stripe price later"/><input type="number" name="sortOrder" defaultValue={100}/><button>Create package</button></form></section>{products.length===0?<p>No sending package products exist.</p>:products.map(p=><article key={p.id} className="rounded border p-4"><h2>{p.name}</h2><p>{p.active?'Active':'Inactive'} — {pricingLabel(p)} — subscribers: {new Set(p.subscriptionItems.map(i=>i.workspaceId)).size}</p><form action={saveProductAction}><input type="hidden" name="id" value={p.id}/><input type="hidden" name="key" value={p.key}/><input name="name" defaultValue={p.name}/><input type="hidden" name="category" value="SENDING_PACKAGE"/><input type="hidden" name="billingInterval" value={p.billingInterval}/><input name="stripePriceId" defaultValue={p.stripePriceId??''}/><input type="number" name="sortOrder" defaultValue={p.sortOrder}/><input type="hidden" name="active" value={String(p.active)}/><input type="hidden" name="recurring" value={String(p.recurring)}/><button>Save package</button></form><form action={setProductActiveAction}><input type="hidden" name="id" value={p.id}/><input type="hidden" name="active" value={String(!p.active)}/><button>{p.active?'Deactivate':'Activate'}</button></form><pre>{JSON.stringify(entitlementMap(p),null,2)}</pre><form action={saveEntitlementAction}><input type="hidden" name="productId" value={p.id}/><select name="entitlementKey">{Object.values(ENTITLEMENT_KEYS).map(k=><option key={k}>{k}</option>)}</select><input name="value"/><button>Save allowance</button></form></article>)}</main>}
