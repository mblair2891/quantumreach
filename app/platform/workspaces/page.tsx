import { PlatformPageTemplate } from "@/components/platform/page-template";
export default function Page(){return <PlatformPageTemplate title="Workspace management" description="Operator-only safe summaries for workspace management." items={['Safe summary','Status','Actions']}/>}
