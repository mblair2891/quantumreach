# Setup queue
`InfrastructureOrder` is the operational queue record. It is sorted by priority then creation time and tracks operator assignment, blockers, customer actions, stage, and customer/operator tasks. It does not calculate deceptive queue positions.
