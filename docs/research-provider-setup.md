# Research Provider Setup

Research uses a provider abstraction controlled by `RESEARCH_ENABLED`, `RESEARCH_PROVIDER`, and `RESEARCH_API_KEY`. When disabled, runs are marked `DISABLED` and UI should show “research provider not configured.” AI-generated insights and interview questions require human review and must not be represented as verified facts.
