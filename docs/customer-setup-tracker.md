# Customer setup tracker
`/setup/status` is user-scoped and renders persisted task/order state rather than a fabricated percentage. It separates waiting on the customer from work that remains with Quantum Reach or an external provider.
