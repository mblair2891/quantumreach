# Scheduling Workflow

Native scheduling pages expose public booking endpoints, collect prospect details, create/update CRM context, block overlapping bookings, and can later call the existing Zoom provider. If Zoom is unavailable, booking confirmation uses a safe manual-meeting fallback and does not break existing manual Zoom meeting creation.
