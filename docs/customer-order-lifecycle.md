# Customer order lifecycle
A `CustomerOrder` joins education enrollment, attribution, payment state, items, setup priority, and an optional workspace. A pending manual order is not paid; only an authorized operational payment-verification action should move it to paid before fulfillment.
