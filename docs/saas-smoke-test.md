# SaaS smoke test

Verify operator access to `/platform`, subscriber denial from `/platform`, subscriber access to `/dashboard`, client redirect from `/dashboard` to `/portal`, public `/pricing`, sending domains in subscriber nav, domain inventory only in platform nav, and no wholesale cost on subscriber pages.
