# Platform subscription operations
`/platform/subscriptions` lists all subscription items and offers a single assignment form. `/platform/workspaces/[workspaceId]` shows products, current infrastructure use, effective entitlements, and a persisted-value trace. Stripe-managed records display a protected read-only message.
