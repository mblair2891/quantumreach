# Customer acquisition funnel
`/start` is the public sales funnel. It deliberately avoids income claims and retains UTM values in its join link. Acquisition sessions are persisted through `recordAcquisition`; callers should provide only a lawful anonymous/session identifier and attribution fields.
