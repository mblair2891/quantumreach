# Known limitations
Manual assignment is an operator override, not a payment or Stripe synchronization replacement. Revenue pages must use verified Stripe invoices/subscriptions rather than manual item quantities. Future-dated non-core assignments are allowed for scheduling but should be reviewed before they activate because core eligibility is evaluated when assigned.
