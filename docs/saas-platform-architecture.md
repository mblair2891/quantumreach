# SaaS platform architecture

Quantum Reach is separated into four personas: Platform Operator, Subscriber, Client, and Public Prospect. Routes are bounded as `/platform/*`, `/dashboard/*`, `/portal/*`, and public paths such as `/meet/*`, `/sign/*`, `/unsubscribe/*`, `/pricing`, `/sign-in`, `/sign-up`, and `/onboarding`.
