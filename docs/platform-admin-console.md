# Platform admin console

`/platform` is operator-only via `ADMIN_EMAILS`. It contains subscribers, workspaces, subscriptions, plans, revenue, domains, email, deliverability, providers, provisioning, jobs, webhooks, audit, and settings. It shows safe aggregate summaries and must not expose secrets.
