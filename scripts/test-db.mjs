import { spawnSync } from "node:child_process";
const required=["DATABASE_URL","DIRECT_DATABASE_URL"];const missing=required.filter(k=>!process.env[k]);if(missing.length){console.error(`Database integration tests require: ${missing.join(", ")}. Use an isolated non-production PostgreSQL database.`);process.exit(2)}if(process.env.TEST_DATABASE_CONFIRMATION!=="quantumreach_test"){console.error("Refusing database tests without TEST_DATABASE_CONFIRMATION=quantumreach_test.");process.exit(2)}for(const [cmd,args] of [["npx",["prisma","migrate","deploy"]],["npx",["vitest","run","--config","vitest.db.config.ts"]]]){const r=spawnSync(cmd,args,{stdio:"inherit",env:{...process.env,NODE_ENV:"test"}});if(r.status!==0)process.exit(r.status??1)}
