const steps = ["Program", "Infrastructure", "Setup speed", "Confirmation"];
export function FunnelProgress({ current }: { current: number }) { return <ol className="mx-auto mb-10 grid max-w-3xl grid-cols-4 gap-2 text-center text-xs font-semibold text-slate-500">{steps.map((step, index) => <li key={step} className={index + 1 <= current ? "text-indigo-700" : ""}><span className={index + 1 <= current ? "mx-auto mb-2 flex h-7 w-7 items-center justify-center rounded-full bg-indigo-600 text-white" : "mx-auto mb-2 flex h-7 w-7 items-center justify-center rounded-full bg-slate-200"}>{index + 1}</span><span className="hidden sm:inline">Step {index + 1} — </span>{step}</li>)}</ol>; }
