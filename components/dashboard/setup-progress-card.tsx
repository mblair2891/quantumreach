import Link from "next/link";
export function SetupProgressCard({ state }: { state: { stage: string; status: string; customerActionRequired: boolean; blocked: boolean; completed: number; total: number } | null }) { if (!state || state.status === "READY") return null; const next = state.customerActionRequired ? "Complete the requested setup information." : state.blocked ? "Quantum Reach is resolving a provider or operator blocker." : "Quantum Reach is preparing your sending infrastructure."; return <section className="rounded-xl border border-sky-200 bg-sky-50 p-4 text-sm dark:border-sky-900 dark:bg-sky-950"><div className="flex flex-wrap items-center justify-between gap-2"><div><h2 className="font-semibold">Setup in progress</h2><p className="mt-1">{state.stage} · {state.completed} of {state.total} milestones complete.</p><p className="mt-1 text-slate-600 dark:text-slate-300">{next}</p></div><Link className="rounded bg-sky-700 px-3 py-2 text-white" href="/setup/status">View setup status</Link></div></section>; }
