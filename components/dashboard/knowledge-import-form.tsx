"use client";

import { useMemo, useState, useTransition } from "react";
import { DOCUMENT_UPLOAD_MAX_FILE_SIZE_BYTES, knowledgeImportAuthorityLevels, knowledgeImportPriorities, knowledgeImportStatuses, knowledgeImportWorkflowStages, TXT_IMPORT_MAX_FILES, validateKnowledgeUploadFile, validateTxtImportFileCount, type KnowledgeImportPreview } from "@/lib/knowledge/import";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";

function FieldLabel({ children }: { children: React.ReactNode }) { return <label className="space-y-1 text-xs font-medium uppercase tracking-wide text-slate-500 dark:text-slate-400">{children}</label>; }
function Select({ value, options, onChange }: { value: string; options: readonly string[]; onChange: (value: string) => void }) { return <select value={value} onChange={(event) => onChange(event.target.value)} className="w-full rounded-xl border border-input bg-white px-3 py-2 text-sm dark:bg-slate-950">{options.map((option) => <option key={option} value={option}>{option}</option>)}</select>; }

export function KnowledgeImportForm({ action, existingDocuments = [] }: { action: (formData: FormData) => Promise<void>; existingDocuments?: Array<{ id: string; title: string; version: string; status: string }> }) {
  const [documents, setDocuments] = useState<KnowledgeImportPreview[]>([]);
  const [message, setMessage] = useState<string | null>(null);
  const [isPending, startTransition] = useTransition();
  const payload = useMemo(() => JSON.stringify({ documents }), [documents]);

  async function loadFiles(fileList: FileList | null) {
    setMessage(null);
    if (!fileList) return;
    const files = Array.from(fileList);
    const countError = validateTxtImportFileCount(files.length + documents.length);
    if (countError) { setMessage(countError); return; }
    const errors = files.map(validateKnowledgeUploadFile).filter(Boolean);
    if (errors.length) { setMessage(errors.join(" ")); return; }
    const formData = new FormData();
    files.forEach((file) => formData.append("files", file));
    const response = await fetch("/api/knowledge/import-preview", { method: "POST", body: formData });
    const result = await response.json() as { previews?: KnowledgeImportPreview[]; errors?: string[]; error?: string };
    if (!response.ok) { setMessage(result.error ?? "Upload preview could not be created."); return; }
    if (result.errors?.length) setMessage(result.errors.join(" "));
    setDocuments((current) => [...current, ...(result.previews ?? [])]);
  }

  function update(index: number, patch: Partial<KnowledgeImportPreview>) { setDocuments((current) => current.map((doc, docIndex) => docIndex === index ? { ...doc, ...patch } : doc)); }
  function toggleStage(index: number, stage: KnowledgeImportPreview["workflowStages"][number]) { setDocuments((current) => current.map((doc, docIndex) => docIndex !== index ? doc : { ...doc, workflowStages: doc.workflowStages.includes(stage) ? doc.workflowStages.filter((item) => item !== stage) : [...doc.workflowStages, stage] })); }
  function submit(formData: FormData) { startTransition(async () => action(formData)); }

  return <div className="space-y-5"><Card><CardHeader><CardTitle>Upload source files</CardTitle><CardDescription>Import up to {TXT_IMPORT_MAX_FILES} TXT, Markdown, PDF, or DOCX files. Each file must be {Math.round(DOCUMENT_UPLOAD_MAX_FILE_SIZE_BYTES / (1024 * 1024))} MB or smaller. Files are parsed server-side for preview before import.</CardDescription></CardHeader><CardContent className="space-y-4"><Input type="file" accept=".txt,.md,.pdf,.docx,text/plain,text/markdown,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document" multiple onChange={(event) => void loadFiles(event.target.files)} />{message ? <p className="rounded-xl border border-amber-300 bg-amber-50 p-3 text-sm text-amber-900 dark:border-amber-800 dark:bg-amber-950/40 dark:text-amber-100">{message}</p> : null}<p className="text-sm text-slate-500 dark:text-slate-400">Default status is <strong>DRAFT</strong>. Choose ACTIVE only after reviewing the source and metadata.</p></CardContent></Card>
  {documents.length ? <form action={submit} className="space-y-4"><input type="hidden" name="payload" value={payload} />{documents.map((doc, index) => <Card key={doc.clientId} className="overflow-hidden"><CardHeader className="border-b dark:border-slate-800"><div className="flex flex-wrap items-start justify-between gap-3"><div><CardTitle className="text-xl">{doc.sourceFileName}</CardTitle><CardDescription>{doc.detectedFileType ?? "FILE"} · {doc.sourceText.length.toLocaleString()} characters · {doc.extractionStatus ?? "EXTRACTED"}</CardDescription></div><Button type="button" variant="outline" onClick={() => setDocuments((current) => current.filter((_, docIndex) => docIndex !== index))}>Remove file</Button></div></CardHeader><CardContent className="space-y-5 pt-5"><div className="grid gap-3 md:grid-cols-2"><FieldLabel>Title<Input value={doc.title} onChange={(event) => update(index, { title: event.target.value })} required /></FieldLabel><FieldLabel>Version<Input value={doc.version} onChange={(event) => update(index, { version: event.target.value })} required /></FieldLabel><FieldLabel>Description<Input value={doc.description} onChange={(event) => update(index, { description: event.target.value })} /></FieldLabel><FieldLabel>Status<Select value={doc.status} options={knowledgeImportStatuses} onChange={(value) => update(index, { status: value as KnowledgeImportPreview["status"] })} /></FieldLabel><FieldLabel>Document type<Select value={doc.documentType} options={knowledgeImportAuthorityLevels} onChange={(value) => update(index, { documentType: value as KnowledgeImportPreview["documentType"] })} /></FieldLabel><FieldLabel>Authority level<Select value={doc.authorityLevel} options={knowledgeImportAuthorityLevels} onChange={(value) => update(index, { authorityLevel: value as KnowledgeImportPreview["authorityLevel"] })} /></FieldLabel><FieldLabel>Priority<Select value={doc.priority} options={knowledgeImportPriorities} onChange={(value) => update(index, { priority: value as KnowledgeImportPreview["priority"] })} /></FieldLabel><FieldLabel>Source file<Input value={doc.sourceFileName} onChange={(event) => update(index, { sourceFileName: event.target.value })} required /></FieldLabel><FieldLabel>New version of<select value={doc.supersedesDocumentId ?? ""} onChange={(event) => update(index, { supersedesDocumentId: event.target.value || undefined, status: doc.status === "ACTIVE" ? "DRAFT" : doc.status })} className="w-full rounded-xl border border-input bg-white px-3 py-2 text-sm dark:bg-slate-950"><option value="">Standalone document</option>{existingDocuments.map((item) => <option key={item.id} value={item.id}>{item.title} · v{item.version} · {item.status}</option>)}</select></FieldLabel></div>{doc.extractionWarnings?.length ? <div className="rounded-xl border border-amber-300 bg-amber-50 p-3 text-sm text-amber-900 dark:border-amber-800 dark:bg-amber-950/40 dark:text-amber-100">{doc.extractionWarnings.map((warning) => <p key={warning}>{warning}</p>)}</div> : null}<div><p className="mb-2 text-xs font-medium uppercase tracking-wide text-slate-500 dark:text-slate-400">Workflow stages</p><div className="grid gap-2 md:grid-cols-3">{knowledgeImportWorkflowStages.map((stage) => <label key={stage} className="flex items-center gap-2 rounded-xl border p-2 text-sm dark:border-slate-800"><input type="checkbox" checked={doc.workflowStages.includes(stage)} onChange={() => toggleStage(index, stage)} />{stage}</label>)}</div></div><details className="rounded-xl border p-3 dark:border-slate-800"><summary className="cursor-pointer text-sm font-medium">Preview source text</summary><p className="mt-3 max-h-72 overflow-auto whitespace-pre-wrap text-xs text-slate-600 dark:text-slate-300">{doc.sourceText}</p></details></CardContent></Card>)}<div className="flex flex-wrap items-center gap-3"><Button type="submit" disabled={isPending || !documents.length}>{isPending ? "Importing…" : `Import ${documents.length} document${documents.length === 1 ? "" : "s"}`}</Button><Button type="button" variant="outline" onClick={() => setDocuments([])}>Clear preview</Button></div></form> : null}</div>;
}
